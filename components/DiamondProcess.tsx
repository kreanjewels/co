'use client'; import {motion,useReducedMotion} from 'framer-motion'; import {useState} from 'react';
export function DiamondProcess(){const reduce=useReducedMotion();const [shape,setShape]=useState('Round');return <div className="mt-10"><motion.div initial={reduce?false:{opacity:0,y:20}} whileInView={reduce?{}:{opacity:1,y:0}} viewport={{once:true}} className="bg-blush p-8"><p className="eyebrow">From rough to finished</p><h2 className="text-3xl mt-3">A stone, thoughtfully shaped.</h2><p className="mt-3">We source, cut, and set with care at every stage.</p></motion.div><div className="mt-8 flex flex-wrap gap-2">{['Round','Oval','Emerald','Pear'].map(s=><button key={s} onClick={()=>setShape(s)} className={`px-4 py-2 border ${shape===s?'bg-ink text-ivory':''}`}>{s}</button>)}</div><p className="mt-4">Selected shape: <strong>{shape}</strong></p></div>}
