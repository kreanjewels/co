import Link from 'next/link'; import {getCollections} from '../../lib/shopify';
export default async function Collections(){const collections=await getCollections();return <div className="container py-20"><p className="eyebrow">Shop</p><h1 className="text-6xl mt-3">Collections</h1>{collections.length===0?<p className="mt-12 p-8 border">No collections found. Connect Shopify credentials to load live data.</p>:<div className="grid md:grid-cols-3 gap-6 mt-12">{collections.map(c=><Link className="bg-white/60 p-6" href={'/collections/'+c.handle} key={c.id}><h2 className="text-2xl">{c.title}</h2><p className="mt-4">{c.description}</p></Link>)}</div>}</div>}
