import {getProduct} from '../../../lib/shopify';
export default async function Product({params}:{params:{handle:string}}){const p=await getProduct(params.handle);if(!p)return <div className="container py-32"><h1 className="text-5xl">Product unavailable</h1><p className="mt-4">This piece could not be found in the Shopify catalogue.</p></div>;return <div className="container py-24 grid md:grid-cols-2 gap-16"><div className="bg-blush aspect-square"/><div><p className="eyebrow">Krean Jewels</p><h1 className="text-6xl mt-4">{p.title}</h1><p className="mt-8 leading-7">{p.description}</p><p className="mt-8 text-xl">{p.priceRange.minVariantPrice.amount} {p.priceRange.minVariantPrice.currencyCode}</p><a href="/contact" className="btn mt-8">Enquire about this piece</a></div></div>}

